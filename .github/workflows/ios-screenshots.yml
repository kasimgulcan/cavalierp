name: iOS App Store Screenshots

on:
  workflow_dispatch:

jobs:
  screenshots:
    runs-on: macos-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Install CocoaPods
        working-directory: mobile/ios
        run: pod install

      - name: Boot iPhone simulator
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcrun simctl list devices available || true

          eval "$(python3 <<'PY'
          import json, subprocess, sys

          def score(name: str) -> int:
              n = name.lower()
              if "pro max" in n: return 100
              if "plus" in n: return 90
              if " pro" in n or n.endswith(" pro"): return 80
              if "mini" in n or "se" in n: return 20
              return 50

          def collect(available_only):
              flag = ["available"] if available_only else []
              data = json.loads(subprocess.check_output(
                  ["xcrun", "simctl", "list", "devices", *flag, "-j"], text=True,
              ))
              out = []
              for runtime in sorted(data["devices"].keys(), reverse=True):
                  if "ios" not in runtime.lower():
                      continue
                  for device in data["devices"][runtime]:
                      name = device.get("name", "")
                      if "iphone" not in name.lower():
                          continue
                      if available_only and device.get("isAvailable") is False:
                          continue
                      out.append((score(name), runtime, name, device["udid"]))
              return out

          candidates = collect(True) or collect(False)
          if not candidates:
              subprocess.run(["xcrun", "simctl", "list", "devices"], check=False)
              raise SystemExit("No iPhone simulator found")

          candidates.sort(key=lambda x: (x[0], x[1]), reverse=True)
          _, runtime, name, udid = candidates[0]
          print(f"Selected: {name} ({udid}) on {runtime}", file=sys.stderr)
          print(f"export DEVICE_ID={udid}")
          print(f"export DEVICE_NAME='{name}'")
          PY
          )"

          echo "DEVICE_ID=$DEVICE_ID" >> "$GITHUB_ENV"
          echo "DEVICE_NAME=$DEVICE_NAME" >> "$GITHUB_ENV"
          xcrun simctl boot "$DEVICE_ID" 2>/dev/null || true
          xcrun simctl bootstatus "$DEVICE_ID" -b
          open -a Simulator || true
          flutter devices

      - name: Build iOS simulator app
        working-directory: mobile
        run: flutter build ios --simulator --debug

      - name: Capture screenshots with flutter drive
        id: drive
        continue-on-error: true
        working-directory: mobile
        env:
          SCREENSHOT_EMAIL: ${{ secrets.SCREENSHOT_DEMO_EMAIL }}
          SCREENSHOT_PASSWORD: ${{ secrets.SCREENSHOT_DEMO_PASSWORD }}
        run: |
          APP_PATH=$(find build/ios/iphonesimulator -name "Runner.app" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "Runner.app not found after build"
            exit 1
          fi
          echo "Using prebuilt app: $APP_PATH"
          flutter drive \
            --driver=test_driver/integration_test.dart \
            --target=integration_test/app_screenshots_test.dart \
            --use-application-binary="$APP_PATH" \
            -d "$DEVICE_ID" \
            --dart-define=SCREENSHOT_EMAIL="$SCREENSHOT_EMAIL" \
            --dart-define=SCREENSHOT_PASSWORD="$SCREENSHOT_PASSWORD" \
            2>&1 | tee ../flutter-drive.log

      - name: Fallback — flutter run + simctl screenshot
        id: fallback
        if: steps.drive.outcome != 'success'
        run: |
          mkdir -p collected-screenshots
          cd mobile
          flutter run -d "$DEVICE_ID" \
            --dart-define=SCREENSHOT_EMAIL="${{ secrets.SCREENSHOT_DEMO_EMAIL }}" \
            --dart-define=SCREENSHOT_PASSWORD="${{ secrets.SCREENSHOT_DEMO_PASSWORD }}" \
            > ../flutter-run.log 2>&1 &
          RUN_PID=$!
          echo "RUN_PID=$RUN_PID" >> "$GITHUB_ENV"

          bash ../scripts/ios_wait_and_screenshot.sh \
            "$DEVICE_ID" \
            ../collected-screenshots \
            01-login

          kill -INT "$RUN_PID" 2>/dev/null || true
          wait "$RUN_PID" 2>/dev/null || true

      - name: Stop flutter run
        if: always()
        run: |
          if [ -n "${RUN_PID:-}" ]; then
            kill -INT "$RUN_PID" 2>/dev/null || true
          fi
          pkill -f "flutter run" 2>/dev/null || true

      - name: Upload drive log on failure
        if: steps.drive.outcome != 'success'
        uses: actions/upload-artifact@v4
        with:
          name: flutter-drive-log
          path: flutter-drive.log
          retention-days: 7
          if-no-files-found: ignore

      - name: Collect screenshot files
        if: always()
        run: |
          mkdir -p collected-screenshots
          for dir in \
            mobile/screenshots \
            mobile/integration_test/screenshots \
            mobile/build/integration_test \
            collected-screenshots
          do
            [ -d "$dir" ] && find "$dir" -maxdepth 3 -name '*.png' -exec cp -n {} collected-screenshots/ \; 2>/dev/null || true
          done
          cd collected-screenshots
          ls -la
          # Require at least one PNG > 80 KB (not blank white)
          ok=0
          for f in *.png; do
            [ -f "$f" ] || continue
            size=$(stat -f%z "$f" 2>/dev/null || stat -c%s "$f")
            echo "$f: $size bytes"
            if [ "$size" -ge 80000 ]; then ok=1; fi
          done
          if [ "$ok" -ne 1 ]; then
            echo "No valid screenshots (need PNG >= 80 KB with visible UI)."
            exit 1
          fi

      - name: Upload screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: app-store-screenshots
          path: collected-screenshots/
          retention-days: 30
          if-no-files-found: warn
